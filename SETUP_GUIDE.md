# SETUP_GUIDE.md — 개발자 사전 준비

> 에이전트가 못 하는, **본인이 직접 해야 하는** 준비를 여기 적는다.
> (외부 서비스 계정 생성, API 키 발급, OAuth 앱 등록, 결제 설정 등 —
> 에이전트는 계정 생성·비밀키 입력을 할 수 없다.)
>
> 프로젝트에 외부 서비스가 없다면 이 파일은 비워두거나 삭제해도 된다.
> `<...>` 부분을 채우고, 채운 뒤 이 안내 줄은 지운다.

## 0. 레포 만들기

1. 빈 레포를 만든다.
2. 이 템플릿 내용을 레포 루트에 넣고 첫 커밋.

## 1. <외부 서비스 A> 준비

<가입·키 발급 절차. 어떤 값을 얻어 어느 환경 변수에 넣는지 명시>

## 2. <외부 서비스 B> 준비

<...>

## 3. .env.local 채우기

Phase 01이 끝나면 `.env.example`이 생긴다. 그것을 `.env.local`로 복사하고
위에서 얻은 값들을 채운다. `.env.local`은 절대 커밋하지 않는다.

## 4. 준비 완료 체크

- [ ] <준비 항목 1>
- [ ] <준비 항목 2>
